export default class Unit10 {
  static title =
    'Utilizando as principais features do workbench (Crash course do workbench)';
  static topics = [
    'Utilizando as principais features do workbench (Crash course do workbench)',
  ];
  static videos = [{ prof: 'Gustavo Caetano', title: 'Workbanch' }];
}
