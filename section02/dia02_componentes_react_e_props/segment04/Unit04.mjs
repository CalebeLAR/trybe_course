export default class Unit04 {
  static title = 'Renderização condicional';
  static topics = [
    'Renderização condicional',
    'Operador Logical AND - &&',
    'Operador ternário - cond ? <A /> : <B />',
    'if/else',
  ];
  static videos = ['FE 2.2 - Renderização condicional'];
  static stackblitz = ['react-exercise'];
}
